<% provide(:button_text, "Edit Kitten") %>
<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<h2>Edit <%= @kitten.name %></h2>
		<%= render 'kittens/form' %>
		<%= link_to "Delete #{@kitten.name}", @kitten, method: :delete, data: {confirm: "Are you sure you want to delete #{@kitten.name}?"} %>
	</div>
</div>